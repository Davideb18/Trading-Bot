---
services:
  freqtrade:
    image: freqtradeorg/freqtrade:stable
    # image: freqtradeorg/freqtrade:develop
    # Use plotting image
    # image: freqtradeorg/freqtrade:develop_plot
    # # Enable GPU Image and GPU Resources (only relevant for freqAI)
    # # Make sure to uncomment the whole deploy section
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
    # Build step - only needed when additional dependencies are needed
    # build:
    #   context: .
    #   dockerfile: "./docker/Dockerfile.custom"
    restart: unless-stopped
    container_name: freqtrade
    environment:
      - FREQTRADE__API_SERVER__JWT_SECRET_KEY=${FREQTRADE_JWT_TOKEN}
    volumes:
      - "./user_data:/freqtrade/user_data"
    # Expose api on port 8080 (localhost only)
    # Please read the https://www.freqtrade.io/en/stable/rest-api/ documentation
    # for more information.
    ports:
      - "8080:8080"
    # Default command used when running `docker compose up`
    command: >
      trade
      --logfile /freqtrade/user_data/logs/freqtrade.log
      --db-url sqlite:////freqtrade/user_data/tradesv3.sqlite
      --config /freqtrade/user_data/config.json
      --strategy NostalgiaForInfinityX7

  risk_manager:
    build:
      context: .
      dockerfile: "./docker/Dockerfile.riskmanager"
    container_name: freqtrade_risk_manager
    restart: unless-stopped
    depends_on:
      - freqtrade
    environment:
      - FREQTRADE_API_URL=http://freqtrade:8080/api/v1
      - FREQTRADE_USERNAME=freqtrade
      - FREQTRADE_PASSWORD=${FREQTRADE_PASSWORD}
      - FREQTRADE_JWT_TOKEN=${FREQTRADE_JWT_TOKEN}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - "./scripts:/app/scripts:ro"
      - "./user_data:/app/user_data:rw"
